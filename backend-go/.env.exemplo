# Aplicação
NODE_ENV=development
PORT=3001

# Banco
DB_HOST=postgres
DB_PORT=5432
DB_NAME=valeris
DB_USER=valeris
DB_PASSWORD=troque_senha_forte

# Segurança
JWT_SECRET=troque_este_jwt_secret_com_no_minimo_32_caracteres
INTEGRATION_TOKENS=
ALLOW_PORTAL_DEMO_TOKEN=false
PORTAL_DEMO_TOKEN=

# CORS
FRONTEND_URLS=http://localhost:3200,http://127.0.0.1:3200

# MinIO
MINIO_ENDPOINT=minio:9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_USE_SSL=false
